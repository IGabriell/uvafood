import { useEffect, useRef, useState } from 'react';
import { MapPin, Clock, ArrowUpRight } from 'lucide-react';
import { Link } from 'react-router';
export function StorefrontSection() { const ref = useRef<HTMLElement>(null); const [seen,setSeen] = useState(false); useEffect(() => { const observer = new IntersectionObserver(([entry]) => setSeen(entry.isIntersecting), { threshold: .25 }); if(ref.current) observer.observe(ref.current); return () => observer.disconnect(); }, []); return <section ref={ref} className="relative overflow-hidden bg-[#120805] py-20 text-white sm:py-28"><div className={`absolute inset-0 bg-cover bg-center transition-transform duration-[1800ms] ${seen ? 'scale-100' : 'scale-110'}`} style={{ backgroundImage: "url('https://images.unsplash.com/photo-1515003197210-e0cd71810b5f?w=1800&h=1200&fit=crop&auto=format')" }} /><div className="absolute inset-0 bg-gradient-to-r from-[#120805] via-[#120805]/75 to-[#120805]/20" /><div className={`relative mx-auto grid max-w-7xl gap-10 px-4 transition-all duration-1000 sm:px-6 lg:grid-cols-[1fr_.85fr] ${seen ? 'translate-y-0 opacity-100' : 'translate-y-12 opacity-0'}`}><div><p className="text-xs font-bold uppercase tracking-[.22em] text-[#ffdb74]">Uma casa no Rio</p><h2 className="mt-4 max-w-xl font-[Fraunces] text-5xl font-bold leading-[.98] tracking-[-.055em] sm:text-6xl">A luz acesa quando a sua fome pede pausa.</h2><p className="mt-6 max-w-lg text-base leading-relaxed text-white/70">Da cozinha aberta à fachada iluminada, nossa unidade em Ipanema é o ponto de encontro entre comida bem-feita e noites que merecem mais sabor.</p><Link to="/restaurante/uvafood" className="mt-8 inline-flex items-center gap-2 rounded-full bg-[#ffdb74] px-5 py-3 text-sm font-bold text-[#35130c] transition hover:scale-105">Conhecer a unidade <ArrowUpRight className="size-4" /></Link></div><div className="self-end rounded-3xl border border-white/15 bg-white/10 p-5 backdrop-blur-md"><p className="font-[Fraunces] text-3xl font-bold tracking-[-.04em]">UVAFOOD <span className="text-[#ffdb74]">Ipanema</span></p><div className="mt-6 grid gap-4 border-t border-white/15 pt-5 sm:grid-cols-2"><p className="flex gap-2 text-sm text-white/75"><MapPin className="size-4 shrink-0 text-[#ffdb74]" />Rua Visconde de Pirajá, 420</p><p className="flex gap-2 text-sm text-white/75"><Clock className="size-4 shrink-0 text-[#ffdb74]" />Hoje, até 23h30</p></div></div></div></section>; }
